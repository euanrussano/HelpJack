package com.sophia.helpjack.model.component

/**
 * Marker interface for anything that can be attached to an Actor.
 * Each component holds only the data/behavior relevant to one trait
 * (sight, carrying wood, hiding, etc.), so an Actor only carries what
 * it actually needs.
 */

interface Component
